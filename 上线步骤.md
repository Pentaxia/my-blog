# 个人博客上线步骤清单

本地博客框架已搭建完成并通过预览验证。以下步骤需要你在自己的电脑和浏览器中手动完成，才能正式上线并被他人访问。

---

## 步骤 1：安装 Git

当前环境中检测到 Git 尚未安装，推送代码到 GitHub 需要它。

1. 访问 https://git-scm.com/download/win
2. 下载安装程序并运行，全部默认选项即可。
3. 安装完成后，打开终端运行：
   ```bash
   git --version
   ```
4. 配置 Git 用户名和邮箱：
   ```bash
   git config --global user.name "你的名字"
   git config --global user.email "你的邮箱"
   ```

---

## 步骤 2：修改博客中的占位信息

打开 `c:\Users\Yu\Desktop\工作\my-blog` 目录下的以下文件，把占位内容替换成你的真实信息：

### `_config.yml`

- `title`：博客名称
- `subtitle`：博客副标题
- `description`：博客简介
- `author`：作者名
- `url`：你购买的域名，例如 `https://yourname.com`

### `_config.next.yml`

- `avatar.url`：可选，替换为你的头像图片路径
- `social`：可选，取消注释并填入你的 GitHub、邮箱等社交链接
- `footer.since`：已设为 2026，可按需修改

### `source/about/index.md`

- 修改联系方式、个人介绍等内容。

---

## 步骤 3：注册 GitHub 并创建仓库

1. 访问 https://github.com 注册账号。
2. 登录后点击右上角 `+` → `New repository`。
3. 仓库名称填写 `my-blog`（可自定义）。
4. 选择 **Public**（公开），然后点击 `Create repository`。
5. 不要勾选初始化 README 等选项，保持空仓库即可。

---

## 步骤 4：将博客代码推送到 GitHub

1. 打开终端，进入博客目录：
   ```bash
   cd c:\Users\Yu\Desktop\工作\my-blog
   ```
2. 初始化 Git 仓库并提交：
   ```bash
   git init
   git add .
   git commit -m "init blog"
   git branch -M main
   git remote add origin https://github.com/你的用户名/my-blog.git
   git push -u origin main
   ```
3. 第一次推送时会提示登录 GitHub，按提示操作即可。

---

## 步骤 5：购买域名

1. 选择域名服务商，例如：
   - Cloudflare Registrar
   - 阿里云
   - 腾讯云
2. 搜索并购买一个喜欢的域名，例如 `yourname.com`、`yourname.cn` 或 `yourname.dev`。
3. 完成实名认证（国内服务商通常需要）。

---

## 步骤 6：在 Vercel 部署博客

1. 访问 https://vercel.com，使用 GitHub 账号登录。
2. 点击 `Add New Project`。
3. 找到并选择你的 `my-blog` 仓库，点击 `Import`。
4. 在配置页面确认以下信息：
   - **Framework Preset**：选择 `Hexo`
   - **Build Command**：`hexo generate`
   - **Output Directory**：`public`
5. 点击 `Deploy`，等待部署完成。

---

## 步骤 7：绑定自定义域名

1. 在 Vercel 项目控制台中，点击 `Settings` → `Domains`。
2. 输入你购买的域名，例如 `yourname.com`，点击 `Add`。
3. Vercel 会提示你需要添加的 DNS 记录，通常类似：
   - 类型：`CNAME`
   - 名称：`www`
   - 值：`cname.vercel-dns.com`
4. 登录你的域名服务商后台，进入该域名的 DNS 管理页面，添加对应的 CNAME 记录。
5. 等待 DNS 生效（通常几分钟到几小时）。
6. 返回 Vercel，确认域名状态变为 `Valid`。

> Vercel 会自动为你的域名申请 HTTPS 证书，无需额外操作。

---

## 步骤 8：验证上线

1. 在浏览器中访问你的域名，确认首页、文章、分类、标签、About 页面都能正常打开。
2. 确认地址栏显示小锁图标（HTTPS 已启用）。
3. 用手机或其他网络环境再访问一次，确保公开可访问。

---

## 后续写作与维护

- 新建文章：
  ```bash
  hexo new post "文章标题"
  ```
- 本地预览：
  ```bash
  hexo server
  ```
- 生成静态文件：
  ```bash
  hexo generate
  ```
- 每次修改后提交并推送，Vercel 会自动重新部署：
  ```bash
  git add .
  git commit -m "update blog"
  git push
  ```

---

## 需要修改的关键文件速查

| 文件 | 作用 |
|------|------|
| `_config.yml` | 站点名称、描述、作者、域名等基础配置 |
| `_config.next.yml` | NexT 主题配置（菜单、侧边栏、社交链接等） |
| `source/about/index.md` | 关于我页面内容 |
| `source/_posts/` | 所有博客文章存放目录 |
| `source/images/` | 图片资源存放目录 |
